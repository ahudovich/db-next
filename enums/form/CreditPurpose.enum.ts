export enum CreditPurpose {
  Purchase = 'purchase',
  Supplement = 'supplement',
  // PreApproval = 'pre-approval', // Currently not in use
  // Consumer = 'consumer', // Currently not in use
  // Business = 'business', // Currently not in use
}
